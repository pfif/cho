pub mod core_types;
pub mod amounts;
pub mod operand_builders_helpers;
// TODO Remove the TODOs before merging
// TODO see if there isn't a better way to organize the code
// TODO work on the documentation a bit
// TODO delete the now unused code (that includes the interfaces that were used for testing)
// TODO add unit tests
// TODO shouldn't all Box<dyn> 's stuff be replaced by Enums?